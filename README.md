# Tclass

This workspace contains two starter projects:

- `web-tclass/` — Next.js frontend project
- `api-tclass/` — Laravel API project

## Setup

### Frontend

```bash
cd web-tclass
npm install
npm run dev
```

### Backend

```bash
cd api-tclass
composer install
php -S localhost:8000 -t public
```
